cube(`Measuretostudy`, {
  sql: `SELECT * FROM public."_MeasureToStudy"`,
  
  joins: {
    
  },
  
  measures: {
    count: {
      type: `count`,
      drillMembers: []
    }
  },
  
  dimensions: {
    
  },
  
  dataSource: `default`
});
